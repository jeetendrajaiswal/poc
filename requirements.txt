# Corporate Annual Report Ingestion PoC
# Standalone — no Django. Python 3.10+

openai>=1.40.0          # OpenAI SDK (files, vector_stores, responses)
pydantic>=2.6.0         # Taxonomy schema + strict structured output models
python-dotenv>=1.0.0    # Load OPENAI_API_KEY etc. from .env
requests>=2.31.0        # Direct /v1/responses calls + response-id deletion
PyYAML>=6.0             # taxonomy / company-profile YAML
PyMuPDF>=1.24           # PDF render (vision) + per-page text
Flask>=3.0              # webapp
Werkzeug>=3.0           # password hashing / uploads
openpyxl>=3.1           # client workbooks (long/wide, review flags)
gunicorn>=21.2          # production WSGI server (Elastic Beanstalk)
boto3>=1.34             # S3 mirror of outputs/job state (survives restarts)
# System dependency (not pip): poppler's `pdftotext` for layout-preserved text.
